name: Build and Release EasyBite

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact: EasyBite
            package: tar.gz
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: easybite.exe
            package: none
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact: EasyBite
            package: tar.gz
          - os: macos-latest
            target: aarch64-apple-darwin
            artifact: EasyBite
            package: tar.gz

    steps:
      - name: 🧾 Checkout source
        uses: actions/checkout@v4

      - name: 🦀 Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: 🛠️ Install Dependencies (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libssl-dev gcc fontconfig libfontconfig1-dev

      - name: 🛠️ Install Dependencies (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          brew update
          brew install pkg-config openssl

      - name: 📦 Run Clippy Fixes
        run: |
          # Automatically fix common issues like unused variables, imports, and mutability
          cargo clippy --fix --allow-dirty || echo "Clippy fixes complete."

      - name: 📦 Build Release Binary (Linux/macOS)
        if: matrix.os != 'windows-latest'
        run: |
          cargo clean
          RUSTFLAGS="-D warnings" cargo build --release --target ${{ matrix.target }} --verbose || {
            echo "Build failed, check logs above for details"
            exit 1
          }

      - name: 📦 Build Release Binary (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $env:RUSTFLAGS="-D warnings"
          cargo clean
          cargo build --release --target ${{ matrix.target }} --verbose || {
            Write-Output "Build failed, check logs above for details"
            exit 1
          }

      - name: 📄 Explain Remaining Errors
        if: failure()
        run: |
          # Explain common errors automatically
          rustc --explain E0252 || echo "No explanation for E0252"
          rustc --explain E0277 || echo "No explanation for E0277"
          rustc --explain E0599 || echo "No explanation for E0599"

      - name: List Build Output (Non-Windows)
        if: matrix.os != 'windows-latest'
        run: |
          echo "Listing contents of ./target/${{ matrix.target }}/release/"
          ls -la ./target/${{ matrix.target }}/release/

      - name: List Build Output (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          Write-Output "Listing contents of .\target\${{ matrix.target }}\release\""
          Get-ChildItem -Path ".\target\${{ matrix.target }}\release\" -Force

      - name: 📦 Package (Linux/macOS)
        if: matrix.package == 'tar.gz'
        run: |
          if [ -f "./target/${{ matrix.target }}/release/${{ matrix.artifact }}" ]; then
            echo "Packaging binary..."
            tar -C target/${{ matrix.target }}/release -czf easybite-v0.3.0-${{ matrix.target }}.${{ matrix.package }} ${{ matrix.artifact }}
          else
            echo "Skipping packaging: binary not found"
          fi

      - name: Skip Packaging for Windows
        if: matrix.os == 'windows-latest'
        run: |
          echo "Skipping packaging step for Windows builds."

      - name: 📤 Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: easybite-${{ matrix.target }}
          path: |
            ${{ matrix.package == 'tar.gz' && 'easybite-v0.3.0-${{ matrix.target }}.${{ matrix.package }}' || './target/${{ matrix.target }}/release/${{ matrix.artifact }}' }}
          if-no-files-found: warn

      - name: 🚀 Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            ${{ matrix.package == 'tar.gz' && 'easybite-v0.3.0-${{ matrix.target }}.${{ matrix.package }}' || './target/${{ matrix.target }}/release/${{ matrix.artifact }}' }}
          body: |
            EasyBite v0.3.0 for ${{ matrix.target }}.
            Install instructions:
            ${{ matrix.os == 'windows-latest' && '```powershell' || '```bash' }}
            ${{ matrix.package == 'tar.gz' && 'curl -L https://github.com/Dangujba/EasyBite/releases/download/v0.3.0/easybite-v0.3.0-${{ matrix.target }}.${{ matrix.package }} | tar -xz -C /usr/local/bin' || '(Copy the binary from the release directory and add it to your PATH)' }}
            ${{ matrix.os == 'windows-latest' && '$env:Path += ";$env:LOCALAPPDATA\\Programs\\EasyBite"' || '' }}
            ${{ matrix.os == 'windows-latest' && '[Environment]::SetEnvironmentVariable("Path", $env:Path, [EnvironmentVariableTarget]::User)' || '' }}
            ${{ matrix.os == 'windows-latest' && '```' || '```' }}
